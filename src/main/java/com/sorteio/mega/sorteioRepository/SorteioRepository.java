package com.sorteio.mega.sorteioRepository;

import com.sorteio.mega.sorteioEntity.SorteioEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SorteioRepository extends JpaRepository<SorteioEntity,Long> {
}
